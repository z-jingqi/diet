---
description: 
globs: 
alwaysApply: false
---
---
description: TypeScript strictness, testing, animations, performance, and forms
globs: ["src/**/*.{ts,tsx}"]
alwaysApply: false
---

# 🧭 Typescript, Testing, and Optimization

- Ensure `tsconfig.json` has **strict** enabled.
- Prefer **interfaces** and `satisfies`, avoid `any` and `enum`; use `as const` and object maps.
- Use **React Testing Library + Vitest/Jest** to reach ≥80% coverage.
- Use JSDoc for complex logic.
- Optimize rendering: `React.memo`, `useMemo`, `useCallback`.
- Lazy-load large components via `React.lazy` + `Suspense`.
- Use **framer-motion** for UI animations.
- Use optimized media (WebP, lazy loaded images).
- Handle forms via **react-hook-form** + `zod` + `@hookform/resolvers`.
